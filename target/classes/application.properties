# itens OBRIGATORIOS
# configurar o usuario
spring.datasource.username = root

# configurar a senha
spring.datasource.password = breno123

# configurar o caminho da base
spring.datasource.url = jdbc:mysql://localhost:3306/projeto?useTimezone=true&serverTimezone=UTC

# configurar o "dialeto"
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# itens OPCIONAIS
# eu quero que o JPA crie as tabelas do banco se elas n�o existirem
spring.jpa.hibernate.ddl-auto = update

# apenas para DEBUG - ele vai mostrar todas as instru��es SQL q ele gera automaticamente
spring.jpa.show_sql = true
